# 开发工作流更新记录

本文记录 `development-workflows` 目录中对各类工作流项目的对比、星标、能力计数与说明文字的更新历史。

## 状态说明

| 状态 | 含义 |
|---|---|
| `已完成` | 已核对并写入文档 |
| `无效` | 发现不成立或无需变更 |
| `搁置` | 结论仍不稳定，暂时保留原值并继续观察 |

---

## 2026-03-19

- `已完成`：更新多个工作流仓库的星标、命令数、技能数与独特性说明。
- `已完成`：修正 `humanlayer`、`context-hub`、`agent-os`、`spec-kit`、`OpenSpec` 等项目的统计口径。
- `已完成`：同步对比报告中的总览表，让 README 与分析报告一致。

## 2026-03-20 至 2026-03-23

- `已完成`：继续刷新 Superpowers、Everything Claude Code、Get Shit Done、gstack、BMAD、Spec Kit 等仓库的最新数据。
- `已完成`：根据星标变化调整排序。
- `已完成`：记录 gstack、GSD、ECC 等项目在命令、技能、代理组织方式上的结构变化。

## 2026-03-24 至 2026-03-29

- `已完成`：持续更新各工作流仓库的星标与技能/代理数量。
- `已完成`：修正部分项目因版本重构导致的统计偏差，例如 BMAD、Compound Engineering、OpenSpec。
- `无效`：个别代理的自动计数出现明显漏算时，保留人工确认结果而非接受错误统计。

## 2026-03-31 至 2026-04-04

- `已完成`：继续跟进 ECC、Superpowers、gstack、GSD、Spec Kit、OpenSpec 等项目的增长趋势。
- `已完成`：当 README、目录结构与脚本输出出现冲突时，优先采用人工复核后的值。
- `搁置`：某些项目在“显式代理”和“隐式子流程”之间的界限并不稳定，数量可能继续波动，因此保留谨慎表述。

---

## 本地化说明

这里保留了项目名、命令名、目录名和版本号的英文写法，因为这些名称本身就是对外可检索的正式标识。
