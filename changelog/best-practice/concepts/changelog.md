# 概念总览更新记录

本文记录 README 中 `CONCEPTS` 与热点条目相关内容的演进，用于说明概念表、链接、徽章与命名的修订历史。

## 状态说明

| 状态 | 含义 |
|---|---|
| `已完成` | 已完成修正并同步到文档 |
| `无效` | 该项无需修正，或经验证当前写法仍可接受 |
| `搁置` | 需要等待进一步确认，但目前不阻塞阅读 |

---

## 2026-03-02

- `已完成`：补充 Agent Teams、Keybindings、Model Configuration、Auto Memory、Checkpointing、Status Line、Remote Control、Fast Mode 等缺失概念。
- `已完成`：修复规则章节与权限页面的链接锚点。
- `已完成`：随后对概念表做收敛，把较细概念折叠进更高层的总类，减少表格膨胀。

## 2026-03-07 至 2026-03-12

- `已完成`：修正多个失效文档链接，例如 `interactive-mode`、`model-config`、`costs` 等页面路径。
- `已完成`：新增 Scheduled Tasks、Code Review 等热点能力条目。
- `无效`：部分 `/slash-commands` 链接虽然表面看起来陈旧，但仍会正确跳转，因此按用户决定保留。
- `已完成`：添加或修复若干徽章资源。

## 2026-03-15 至 2026-03-20

- `已完成`：统一“Subagents”等命名，增强与官方术语的一致性。
- `已完成`：修正 Hooks 描述，明确其已不只是“确定性脚本”，还可以是 HTTP、提示词与代理型处理器。
- `已完成`：将 Voice Mode 调整为更准确的 Voice Dictation，并改为指向正式文档。
- `已完成`：加入 Channels、Auto Mode 等新热点条目。

## 2026-03-21 至 2026-03-26

- `已完成`：持续验证外部链接、徽章路径与关键锚点。
- `已完成`：增加 Slack 集成、GitHub Actions / CI-CD 等新能力入口。
- `已完成`：把“Bundled Skills”等链接更新到更准确的官方页面。
- `无效`：少数历史路径虽然并非最新叫法，但由于仍有效且已有兼容跳转，未做强制替换。

---

## 本地化说明

为保证仓库中的链接、路径与命令仍可直接使用，下列内容保留英文：

- 文档路径、锚点与 URL
- 命令名、目录名、配置键名
- 产品与功能专名，例如 Hooks、Channels、Auto Mode
